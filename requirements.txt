# MirAI_OS – Python dependencies
# Install with:  pip install -r requirements.txt

# ---------------------------------------------------------------------------
# Core – always required
# ---------------------------------------------------------------------------

# HTTP client (OpenRouter / external APIs)
httpx>=0.27.0

# Environment / secrets
python-dotenv>=1.0.0

# ---------------------------------------------------------------------------
# lab_edition.py – Unified AI Agent Orchestrator
# ---------------------------------------------------------------------------

# FastAPI REST API server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Docker SDK (optional – gracefully degraded if not installed)
# docker>=7.0.0

# Kubernetes SDK (optional – gracefully degraded if not installed)
# kubernetes>=28.1.0

# ---------------------------------------------------------------------------
# Telegram bot integration (optional)
# ---------------------------------------------------------------------------
# python-telegram-bot>=20.7

# ---------------------------------------------------------------------------
# Voice I/O (optional)
# ---------------------------------------------------------------------------
# Text-to-Speech
# edge-tts>=6.1.9

# Speech-to-Text
# SpeechRecognition>=3.10.0
# PyAudio>=0.2.14

# ---------------------------------------------------------------------------
# SSH / Codespace connector (optional)
# ---------------------------------------------------------------------------
# paramiko>=3.4.0

# ---------------------------------------------------------------------------
# Kubernetes orchestration (optional)
# ---------------------------------------------------------------------------
# kubernetes>=28.1.0

# ---------------------------------------------------------------------------
# Local LLM support (optional, for Raspberry Pi / offline mode)
# ---------------------------------------------------------------------------
# transformers>=4.40.0
# torch>=2.0.0

# ---------------------------------------------------------------------------
# Build executable (Windows)
# ---------------------------------------------------------------------------
# pyinstaller>=6.6.0
